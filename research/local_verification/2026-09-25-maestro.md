# Local verification: maestro environment, 2026-09-25

**Measured.** The protocol from `ca2acdf` ran in the `maestro` Conda environment
(Python 3.11.16) against `fcc38a4` plus the TypeSafe Jev contract fix later
committed as `22f1090`. The full machine-local artifacts are git-ignored under
`outputs/local_verification/`; this file records the findings that can be shared
without credentials or local asset data. The fixture case is synthetic and says
nothing about a biological target or mechanism.

## Offline phases 0-3

The suite reported **1167 passed, 54 failed, 0 errors, 1 skipped**. All eleven
acceptance checks passed. Relative to the no-local-assets baseline, 53 failing
test IDs remained. The one new ID was
`tests/test_repository_shape.py::test_project_markdown_has_no_chinese_prose`:
the local `dataset/external/MAP/MAP/baseline/README.md` contains only a newline.
The baseline collection error `tests.test_learned_response` disappeared with
`torch` installed. The fixture's real-result-import check passed even though
`data/` was absent.

## Live phase 4

The [TypeSafe API](https://docs.typesafe.ai/api) accepted the repaired request.
Jev served `jev-1.13.0` and returned usable Noul, Choice and Score answers with
no refusals. The critic loop recorded five judgments, all labelled
`model_prediction`; the deterministic check and selected action (`rna_high`)
were unchanged from the critic-off trace. The comparison verdict was
`the critic stayed advisory on this case`.

The probe currently makes one raw request and a second request through
`evaluate()`. Their Choice answers differed (`proximal_activity` and
`orthogonal_rescue`), so the two sections of its report must not be read as one
response. Their respective token usages were 517 input / 112 output and 517
input / 114 output. This is a limitation of the probe, not a Jev refusal.

## Live phase 5 and asset gates

All three template-free runs stopped at task interpretation with
`no_executable_action`: the short prompt lacked target, intervention, biological
context, phenotype endpoint and existing observation. The planner contract and
single back-prompt were not reached in any run. All three selected no action;
none invented an action ID. Per-run token usage is not retained in the traces
or event logs, so it could not be reported. This protocol invocation therefore
does not measure the live planner's contract-violation rate or recovery rate.

Phases 6 and 7 were skipped because neither
`data/virtual_cell/registry.json` nor `data/evaluation/cases/real/` exists on
the verifying machine.
