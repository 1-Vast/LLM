# Typed decision review

Validates a recorded Typed Jev review as an advisory model output. It checks the closed scopes,
question kinds, evidence origin and non-measurement flags, reports unstable scopes, and preserves
the deterministic validator as the authority for premises, belief updates, and final action
selection. It never calls the provider and never treats a judgment as a measurement.
