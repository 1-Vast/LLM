# Column Summary

Computes count, missingness, minimum, maximum, mean, median, and sample standard deviation for
one explicitly named numeric column in a CSV or TSV file. It is descriptive only; it does not
perform hypothesis testing, adjustment, or causal analysis.

Entry point: `tool.py`, function `run(parameters)`. Required parameter: `column`.
