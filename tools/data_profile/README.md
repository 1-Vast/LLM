# Dataset Profile

Profiles CSV, TSV, and JSON files before more specific analysis. It reports dimensions, visible
fields, a bounded sample, and missing-value counts. The output describes file content only and
does not validate source quality or support a biological mechanism claim.

Entry point: `tool.py`, function `run(parameters)`. Optional parameter: `sample_rows` from 1 to 25.
